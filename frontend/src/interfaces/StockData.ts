export interface StockData {
    price: number;       // The current price of the stock or crypto
    timestamp: string;   // The timestamp of the data entry
    // Add any other fields you need to capture for each stock/crypto data point
}
